services:
  tunnel-client:
    build: .
    container_name: tunnel-client
    restart: unless-stopped
    ports:
      - "3002:3000"
    volumes:
      # Persist credentials between restarts
      - ./credentials.json:/app/credentials.json
      # frpc config (auto-generated)
      - frpc-config:/etc/frp
      # frpc logs
      - ./logs:/tmp
    environment:
      - TZ=UTC
      - SERVER_URL=http://tunnel.ersantana.com:8000

volumes:
  frpc-config:
